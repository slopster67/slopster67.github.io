{
  "name": "slopster67.github.io",
  "version": "1.0.0",
  "private": true,
  "description": "Personal website for slopster",
  "scripts": {
    "dev": "rm -rf _site && npx @11ty/eleventy --serve",
    "build": "rm -rf _site && npx @11ty/eleventy"
  },
  "devDependencies": {
    "@11ty/eleventy": "^3.0.0"
  }
}
