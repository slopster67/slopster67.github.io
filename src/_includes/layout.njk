<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>{% if title %}{{ title }} | {% endif %}slopster</title>
    <meta name="description" content="{{ description or 'slopster crashed because of reckless hydration crimes.' }}" />
    <link rel="stylesheet" href="/css/site.css" />
  </head>
  <body>
    {{ content | safe }}
  </body>
</html>
